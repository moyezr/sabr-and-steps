# Source-grounded draft acceptance — 2026-09-15

Authenticated OpenRouter indexed all 293 available Quran Foundation pre-live passages, using `openai/text-embedding-3-small`, 1536 dimensions, preprocessing `translation-text-v1`. Index/config/content checksums are persisted; no local inference is used.

Evaluation job `51a8192e-2a39-4a4c-be95-5c9c249f3e74` passed four top-five retrieval cases (2:153, 2:152, 2:155, 2:183) and an unsupported JavaScript compiler query returning no sources. Results are preserved locally at `.data/jobs/<job>/retrieval-evaluation.json`. This is a small labeled smoke evaluation over partial coverage, not a claim of broad theological retrieval quality.

Draft job `254df49d-c41d-403b-b0e5-337b253e6c92` generated an original reflection through `openai/gpt-5.6-luna`. The model supplied a canonical source ID with empty quotation wording; the application inserted the immutable translation of 2:153. A provider model naming difference (`text-embedding-3-small`) was initially rejected, then explicitly normalized to the exact documented model alias. Cached responses were reused rather than dispatching a duplicate.

Production browser: opened the pilot script, inspected attribution/context 2:151–155, changed Reflection 7 to a concrete act of care, saved, and reloaded. A new unreviewed revision persisted. The quotation remained read-only and source-linked. No creator review was falsely recorded.

Actual ledger cost for indexing, evaluation and draft generation: **$0.00214733**. Provider responses, retrieval artifacts and corpus outputs are ignored local data. Edition reuse remains `not_cleared`, with coverage limited to chapters 1–2 (293 verses).

Verification: `pnpm check` (15 tests), `pnpm test:db` (3 suites), and `pnpm build` passed. Unit/integration fixtures reject fabricated source IDs, altered quotation wording, configuration drift and stale revisions; editing invalidates source/script review. Durable-job checks cover duplicate claims, deduplication, expired lease recovery and uncertain dispatch. Build currently reports a broad file-tracing warning, tracked in PROGRESS; it does not prevent compilation but still needs resolution.
